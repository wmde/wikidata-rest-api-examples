meta {
  name: POST-item-printer
  type: http
  seq: 18
}

post {
  url: {{edit-url}}/entities/items
  body: json
  auth: inherit
}

body:json {
  {
    "item": {
      "labels": {
        "en": "printer"
      }
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
