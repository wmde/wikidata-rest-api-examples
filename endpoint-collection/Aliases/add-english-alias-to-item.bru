meta {
  name: Add English Alias to Item
  type: http
  seq: 21
}

patch {
  url: {{base-url}}/entities/items/{{sandbox-id}}/aliases
  body: json
  auth: inherit
}

body:json {
  {
    "patch": [
      {
        "op": "add",
        "path": "/en/-",
        "value": "sandbox item"
      }
    ],
    "tags": [],
    "bot": false,
    "comment": "Add English alias"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
