meta {
  name: Change English Alias of Item
  type: http
  seq: 2
}

patch {
  url: {{base-url}}/entities/items/{{sandbox-id}}/aliases
  body: json
  auth: inherit
}

body:json {
  {
    "patch": [
      {
        "op": "add",
        "path": "/en/-",
        "value": "sandbox item"
      }
    ],
    "tags": [],
    "bot": false,
    "comment": "Add English alias"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
