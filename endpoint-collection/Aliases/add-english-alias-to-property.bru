meta {
  name: Add English Alias to Property
  type: http
  seq: 22
}

patch {
  url: {{base-url}}/entities/properties/{{property-id}}/aliases
  body: json
  auth: inherit
}

body:json {
  {
    "patch": [
      {
        "op": "add",
        "path": "/en/-",
        "value": "sandbox property"
      }
    ],
    "tags": [],
    "bot": false,
    "comment": "Add English alias"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
