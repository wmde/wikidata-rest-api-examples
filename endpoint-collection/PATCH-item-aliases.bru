meta {
  name: PATCH-item-aliases
  type: http
  seq: 26
}

patch {
  url: {{base-url}}/entities/items/{{sandbox-id}}/aliases
  body: json
  auth: inherit
}

body:json {
  {
    "patch": [
      {
        "op": "add",
        "path": "/en/-",
        "value": "sandbox item"
      }
    ],
    "tags": [],
    "bot": false,
    "comment": "Add American English alias"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
